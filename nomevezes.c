#include <stdio.h>
#include <string.h>
// repetir o nome segundo o nomero de caracteres
main()
{
	char nome[50];
	int i;
	puts("insira um nome");
	gets(nome);
	for(i=0;i<=strlen(nome);i++)
	printf("%s\n",nome);





}
